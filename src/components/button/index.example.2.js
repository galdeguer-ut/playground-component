const buttonExample = `
const onClick = () => {
  alert('Button clicked');
};

render(
  <div>
    <Button onClick={onClick}>
      Click me
    </Button>
  </div>
);
`.trim();

export default buttonExample;
